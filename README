1. Update testing environment in configration.cfg.
	TARGET_IP=192.168.2.2
	TARGET_TYPE=wp76xx
	WIFI_ACCESSPOINT=TMA_WIFI

2. Run ./run.sh 2>&1 | tee -a test.log
3. Check Result of Testing in the end of running:
	If the the message [PASSED] -> Testing is Passed
	If the the message [FAILED] -> Testing is Failed
